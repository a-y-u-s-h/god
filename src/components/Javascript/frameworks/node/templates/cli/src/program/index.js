import csv from "@/components/elements/csv"
import text from "@/components/elements/text"
import shell from "@/components/elements/shell"

export default async options => {
  /*
    ======================================
      Start writing your program logic
      here if you're trying to work simply
      with command line options. If options is
      an empty object, this function will
      not run at all.
    ======================================
  */
}

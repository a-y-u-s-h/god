let data = {
  sketch: {
    container: {
      id: 'canvas'
    }
  }
}

/* :: -------------------------------------- :: */


(s => {
  
  s.setup = () => {

  }

  s.draw = (dt) => {
    
  }

  s.run()


}) (new Pixi(data.sketch.container.id))
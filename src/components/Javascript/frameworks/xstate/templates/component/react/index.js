import React from "react"
import System from "./system"

export const Placeholder = () => {
  const system = System.local()
  const { state, settings } = system
  return <></>
}

export default Placeholder
